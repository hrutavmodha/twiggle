{
    "name": "esbuild-plugin-twiggle",
    "version": "1.6.4",
    "description": "esbuild plugin for Twiggle JSX transformation",
    "main": "dist/index.js",
    "type": "module",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsup src/index.ts --format cjs,esm --dts",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [
        "esbuild-plugin",
        "twiggle",
        "jsx"
    ],
    "author": "Hrutav Modha",
    "license": "MIT",
    "devDependencies": {
        "typescript": "^5.0.0",
        "esbuild": "^0.20.0",
        "tsup": "^8.0.1"
    },
    "dependencies": {
        "twiggle-plugin-core": "^1.6.0"
    }
}
