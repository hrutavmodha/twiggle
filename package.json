{
    "name": "twiggle-monorepo",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build:plugin": "npm run build --workspace=vite-plugin-twiggle",
        "build:twiggle": "npm run build --workspace=twiggle",
        "build": "npm run build:plugin && npm run build:twiggle"
    },
    "devDependencies": {
        "@typescript-eslint/eslint-plugin": "^8.46.2",
        "@typescript-eslint/parser": "^8.46.2",
        "eslint": "^9.38.0",
        "eslint-config-prettier": "^10.1.8",
        "eslint-plugin-prettier": "^5.5.4",
        "prettier": "^3.6.2"
    }
}
